const { exec } = require('child_process');

exports.handler = async (event, context) => {
    // let commonbdat = document.getElementById("commonbdat").value; // node js cant access this 
    // exec(`./linuxBDAT info ${commonbdat} -t TableName'`)
    console.log("Just see what happens");
  }
